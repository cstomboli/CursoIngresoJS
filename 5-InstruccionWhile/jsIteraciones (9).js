function mostrar()
{

	var contador=0;
	// declarar variables
	var numero;
	var maximo;
	var minimo;
	var respuesta='si';

	while(respuesta!='no')
	{
		numero=prompt("Ingrese un numero:");
		if (numero ==0)
		{
			maximo = numero;
			minimo=numero;
		}
		else
		{
			if (numero>maximo)
			{
				maximo=numero;
			}
			if (minimo<numero)
			{
				minimo=numero;
			}
		}
	numero ++;

	respuesta=prompt("Desea ingresar otro numero?");
	
	}


	Error !! minimo=minimo.value;
	maximi=maximo.value;

	while (respuesta !="no")
		


}//FIN DE LA FUNCIÓN